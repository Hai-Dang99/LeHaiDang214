@model LeHaiDang214.Models.LHD0214

@{
    ViewData["Title"] = "Edit";
}

<h1>Sửa</h1>

<h4>LHD0214</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="LHDID" />
            <div class="form-group">
                <label asp-for="LHDName" class="control-label"></label>
                <input asp-for="LHDName" class="form-control" />
                <span asp-validation-for="LHDName" class="text-danger"></span>
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="LHDGender" /> @Html.DisplayNameFor(model => model.LHDGender)
                </label>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Trở lại</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
